Algorithme status
Variables 
    m, n : Réel
Debut 
    Ecrire ("Entrer le premier nombre: ")
	Lire (m)
	Ecrire ("Entrer le deuxieme nombre: ")
	Lire (n)
    Si (n = 0 | m = 0) alors
        Ecrire ("La multiplicatoin de deux nombre egale a 0") 
    sinon 
        si ((n < 0 & m > 0 )|(n > 0 & m < 0 )) alors
            Ecrire ("La multiplicatoin de deux nombre est negative")
        sinon 
            Ecrire ("La multiplicatoin de deux nombre est positive")
        finsi
    finsi
Fin
